import { MyTaskDirective } from './my-task.directive';

describe('MyTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new MyTaskDirective();
    expect(directive).toBeTruthy();
  });
});
